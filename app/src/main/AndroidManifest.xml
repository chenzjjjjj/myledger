<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.chenzj.myledger">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.MyLedger">
        <activity android:name=".view.AddLedgerActivity">
        </activity>
        <activity android:name=".view.BillActivity">
        </activity>
        <activity
                android:name=".view.LedgerActivity"
                android:label="@string/title_activity_ledger">
        </activity>
        <activity
                android:name=".view.SignUpActivity"
                android:theme="@style/Theme.MyLedger.NoActionBar">
        </activity>
        <activity
                android:name=".view.LoginActivity"
                android:theme="@style/Theme.MyLedger.NoActionBar">
        </activity>
        <activity
                android:name=".view.MainActivity"
                android:label="@string/app_name"
                android:theme="@style/Theme.MyLedger.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>